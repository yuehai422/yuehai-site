# Cloudflare Workers static assets (Git-connected deploy).
# Build runs first (hugo → public/), then: npx wrangler deploy
name = "yuehai-site"
compatibility_date = "2026-09-22"
workers_dev = true

[assets]
directory = "./public"
